global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  # Scrape OTel Collector's Prometheus exporter (receives metrics from instrumented services)
  - job_name: 'otel-collector'
    static_configs:
      - targets: ['otel-collector:8889']
